pipeline {
    agent any
    stages {
        stage('first stage') {
            steps {
            echo "the git commited id :$(GIT_COMMIT)"
            echo "creating branch :$(GIT_BRANCH)"
            }
        }
    }
}

    
